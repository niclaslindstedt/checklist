// Shared props for the inline SVG icon set. Each icon takes a `className`
// so callers control size and colour through Tailwind utilities (icons
// paint with `currentColor`).
export type IconProps = { className?: string };
