---
type: Added
title: Build version label
---

A version label in the header menu (next to "View source") shows the running build — semver plus the CI build number and commit hash — so you can tell at a glance which version you're on.
