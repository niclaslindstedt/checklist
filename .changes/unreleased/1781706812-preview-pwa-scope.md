---
type: Fixed
title: Preview/branch PWA isolation
---

Installing the app from the `/preview/` or `/branch/` slot now runs that slot's build instead of silently falling through to production, and each installs as its own distinctly named home-screen app.
