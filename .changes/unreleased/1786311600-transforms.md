---
type: Added
title: Transforms
doc: transforms
---

Settings → Transform lets each namespace have its own rules that rewrite how your items read — turning a reference like `#134` into a link to that issue, swapping one phrase for another, or masking a code as `076****123` — without changing the text your lists actually store.
